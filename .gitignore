node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*.local
.DS_Store
.render-demo
.shadow-eval/
.grid-check/
.bakeoff-s03/
eval/mixer/.render/
eval/mixer/artifacts/
eval/export/.render/
eval/export/artifacts/
eval/chord/.render/
# 2O-B.1 launch audio + headroom render bundle
eval/chord-audio/.render/
eval/expression-benchmark/.render/
# 2T-C §10 guitar-performance render bundle
eval/guitar-performance/.render/
# 2U-A §1: the golden baseline is committed; each run's readout is not
eval/ui-contract/CURRENT.json
# 2S-A intent-composer render bundle
eval/intent-composer/.render/
# 2V-C.4 §3 rendered-PCM seam bundle
eval/expression-fidelity/.render/

# Probe protokolünün geçici yedeği (2Q-B §17). Asla commit edilmez.
*.probebak

# 2V-A §10: the last run's readout and its founder screenshots are committed;
# the per-run copies of the ten-run sweep are summarised in RUNS.json instead
eval/editor-2va/artifacts/RESULTS-run*.json
eval/editor-2va/artifacts/failed-*.png

# 2V-A.1: the last run's readout and its founder screenshots are committed;
# per-run copies of the ten-run sweeps are summarised in RUNS.json instead
eval/editor-2va1/artifacts/*-run*.json
eval/editor-2va1/artifacts/failed-*.png
